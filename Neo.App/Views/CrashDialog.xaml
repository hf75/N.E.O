<Window x:Class="Neo.App.CrashDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="What do you want to do?" FontFamily="Segoe UI Semilight"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        ShowInTaskbar="False" Topmost="True">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Main Content Area -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Icon (using a simple TextBlock as a placeholder) -->
            <TextBlock x:Name="IconText" Grid.Column="0" Text="⚠️" FontSize="48" VerticalAlignment="Center"/>

            <!-- Message Text -->
            <TextBlock x:Name="MessageText" Grid.Column="1" Text="Message goes here." TextWrapping="Wrap" VerticalAlignment="Center"/>
        </Grid>

        <!-- Button Area -->
        <Border Margin="0 6" Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Button Style="{StaticResource RoundedButton}" Grid.Column="0" x:Name="Button1" Content="Button 1" Margin="5" Click="Button1_Click" IsDefault="True"/>
                <Button Style="{StaticResource RoundedButton}" Grid.Column="1" x:Name="Button2" Content="Button 2" Margin="5" Click="Button2_Click"/>
                <Button Style="{StaticResource RoundedButton}" Grid.Column="2" x:Name="Button3" Content="Button 3" Margin="5" Click="Button3_Click" IsCancel="True"/>
            </Grid>
        </Border>
    </Grid>
</Window>