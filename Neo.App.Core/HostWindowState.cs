namespace Neo.App
{
    /// <summary>
    /// Cross-platform window state enum, replacing System.Windows.WindowState.
    /// </summary>
    public enum HostWindowState
    {
        Normal,
        Minimized,
        Maximized
    }
}
